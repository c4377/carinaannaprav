# Netlify-Konfiguration

[build]
  publish = "."
  functions = "netlify/functions"

# Scheduled Function: TidyCal alle 30 Minuten auf neue Buchungen pruefen
[functions."tidycal-telegram"]
  schedule = "*/30 * * * *"
